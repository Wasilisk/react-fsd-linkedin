import { styled } from "shared/libs/mui/styled";
import { Box } from "shared/ui/box";

export const UserCardExtraActions = styled(Box)({
    position: "absolute",
    top: 0,
    right: 0,
    margin: "5px 5px 0 0",
    zIndex: 10
})