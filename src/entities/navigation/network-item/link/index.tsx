import { styled } from "shared/libs/mui/styled";
import { Link } from "shared/libs/router/link";

export const NetworkNavigationLink = styled(Link)({
    width: "100%",
    textDecoration: "none",
    color: "inherit",
    position: "relative",
}) 